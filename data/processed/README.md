# Processed Data Directory

Contains feature-extracted data and chronologically split train, validation, and held-out test datasets:

- `train.csv` / `train.parquet`
- `val.csv` / `val.parquet`
- `test_heldout.csv` / `test_heldout.parquet`
