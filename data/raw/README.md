# Raw Data Directory

Place raw transaction event logs (CSV, JSON, Parquet) in this folder.

Raw data contains unmodified transaction payloads prior to feature extraction and sliding window aggregation.
